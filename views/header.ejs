<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="flex justify-between py-4 px-8 xl:px-64 2xl:px-96 border-b items-center shadow-sm">
      <div>
        <h1 class="text-2xl font-bold"><a href="/"> Cosmere Club</a></h1>
      </div>
      <div class="flex gap-6">
        <% if (currentUser) { %>
        <a href="/log-out">Log Out</a>
        <a href="/new-message">Create New Message</a>
        <% } else { %>
        <a href="/sign-up">Sign up</a>
        <a href="/log-in">Log In</a>
        <% } %> <% if (currentUser?.membershipStatus === "visitor") { %>
        <a href="/member">Become member</a>
        <% } %> <% if (currentUser?.membershipStatus === "member") { %>
        <a href="/admin">Become Admin</a>
        <% } %>
      </div>
    </div>
